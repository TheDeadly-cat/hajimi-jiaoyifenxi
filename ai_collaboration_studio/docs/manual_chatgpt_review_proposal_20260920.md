# ChatGPT 导入后的独立复核方案（待新授权）

本方案只做离线准备，新增模型调用为 0。历史三次连通性请求已经消费完毕，不能抵作这份 ChatGPT 结果的复核，也不能重置其数据库账本。OpenAI API 继续硬禁用；千问个人编程套餐不接入后端。

## 已冻结材料

- 会话 `mcg_4f68dcbd3fe842bc6ea9b929`，standard 模式；已导入，状态 `API_REVIEW`，实际独立复核仍为 `NOT_STARTED`。
- bundle：`5f311f0949837268a57b795fe74b8f2e9d1e3933ca157cb76dd05cca3e4d1dfd`。
- context：`4045b0ed77e7d192975c42579f9d4bada2c537cfbb5014f734d51c2ddc5c6e5c`。
- 规范化结果：`332e48ab2772b516b01e24664cc3f2a6a932d738260fa98549977a6fa500b427`。
- 每次发送同一冻结目标、2 份共 2,481 字符的选段、证据 ID、候选矩阵，以及已导入的全部两轮 Panel 和最终综合结果。不同请求只改变复核任务及序号；不发送之前的 API 试验答案或其他复核者的答案。
- 正文覆盖依然是 3/16 段，13 段和附件未读，不新增来源抓取。用户声明 `gpt6pro` 保留为未验证元数据，8 个角色视角不算 8 个独立模型。

使用保存的 `imported-session.json` 及导入回执离线生成请求，并核对文件哈希及规范化结果哈希；未打开 SQLite、运行导入驱动或调用 Provider。9 份请求文件是三个互斥候选各三份的**静态预览**，不表示批准 9 次请求。

## 推荐方案与候选

沿用用户已确认的平台。优先候选为豆包 `doubao-seed-2-1-pro-260915`，通过方舟官方 Responses 端点。当前服务一次复核运行绑定一个 provider/model，三项任务依次是事实核对、反方审查、风险审查，每项最多一次，**共最多 3 次，每次最多 1,400 输出 Token，总上限 4,200**。这是同一模型的三次独立请求，不是三个独立模型的投票；也不证明盲审独立性。

| 互斥候选 | 平台 / 模型 | 输入、输出单价（每百万 Token） | 三次规划估算 | 按完整 HTTP 字节加余量估算 | 建议消费计划限额 |
| --- | --- | --- | --- | --- | --- |
| 优先候选 | 方舟 / `doubao-seed-2-1-pro-260915` | CNY 6 / 30 | CNY 0.244812 | CNY 0.540990 | CNY 0.60 |
| 暂不可执行 | 方舟 / `glm-5-2-260617` | CNY 8 / 28 | CNY 0.276016 | CNY 0.668736 | CNY 0.75 |
| 次要候选 | DeepSeek 官网 / `deepseek-v4-pro` | USD 1.32 / 3.96 | USD 0.04277064 | USD 0.10791792 | USD 0.12 |

常规估算逐次约 6,599–6,602 输入 Token，采用已有 `cjk_one_ascii_four_v1` 方法；保守估算采用完整 HTTP 请求 UTF-8 字节数加 256，三项逐次计费，未扣缓存或免费额度。两种方法都不是供应商 tokenizer 或账单硬保证。旧冻结预算只估计研究上下文，没有计入后续真实导入全文，不能直接拿旧输入 Token 预算审批。

豆包/方舟智谱暂沿用本项目最近一次官方页面/用户控制台截图核对的单价，最终执行前须重新核对。此次方舟价格文档未能通过只读检索重新完整读取，不能称为实时价格已验证。入口：[方舟价格](https://docs.volcengine.com/docs/ark/model-pricing)。DeepSeek 已于 2026-09-20 重新读取[官方价格页](https://api-docs.deepseek.com/quick_start/pricing/)，使用峰时、未命中缓存价格估算，不预先计算优惠。

豆包当前原生 JSON 路径显式关闭 thinking；HTTP 超时参数为 240 秒。DeepSeek 当前 JSON 路径超时参数为 180 秒。GLM 和 DeepSeek 的推理可能占用输出预算，1,400 Token 能否完成此类审查尚无真实证据；截断、空结果或合同不符即停止，不能自动增大上限、换模型或补跑。

方舟智谱存在具体适配缺口：`ManualChatGPTService.run_api_review()` 优先调用可调用的 `generate_json()`，而 `ArkGLMProvider.generate_json()` 明确返回 `output_mode_not_supported`；其有效路径是 prompt-JSON 的 `generate()`。此前单次证据试验已正确选择该路径，不意味着当前手工复核服务已经兼容。若后续选定 GLM，需先离线补齐按声明能力选择方法的定向修复与验证。本阶段没有改动该流程或绕过合同。

## 豆包具体请求预览

端点固定为 `https://ark.cn-beijing.volces.com/api/v3/responses`，方法 POST，`store=false`，原生 JSON，输出上限 1,400；无工具、抓取或额外 probe。

| 任务 | 完整 HTTP 字节 | 请求体 SHA-256 |
| --- | ---: | --- |
| 事实核对 | 22,794 | `2e3fddad64436a39d62a220df9d5a4d419c59e9548c336dd249ea7f527c7fe71` |
| 反方审查 | 22,804 | `02e53ac9e5e91e57073f0f22ab536e62653b4cf3ca39982554eaf3fd462d6d0b` |
| 风险审查 | 22,799 | `04ee094435fe5986a43123e1ea425225117e5e6aeda82c1579bd8944a6551aa9` |

完整请求和其他候选预览仅存本机 `offline-closeout-20260920/review-preview-v2`，不上传真实模型结果或完整研究材料。第一版本机预览器误传端点枚举而被拒绝的日志也保留；这是离线辅助脚本错误，不是生产调用失败，更没有发生付费。

## 后续执行边界

建议有效窗口为用户明确批准具体摘要起 30 分钟；每次出网前检查时限，失败、超时、无效返回均占用一次并立即停止，不自动重试。将使用现有独立手工副本，绑定上述结果和新的复核账本；不复制或清零已经用满的 API 试验库。

这份方案**不是可执行授权摘要**。当前普通手工复核入口已有次数、输出上限及幂等账本，但没有本方案要求的完整 HTTP 哈希、金额与过期时间联合执行门禁。新增付费前，须先离线完成受控执行包装、固定最终干净源码 SHA、价格核对、请求摘要及拒绝路径验证，随后仅请求用户批准这一份具体计划。不得直接拿本草案调用普通入口，也不重复询问已确认的平台偏好。

密钥只在获批后由用户于本机圆点密码框输入，仅当前进程使用。成功复核仍属于模型建议，保留所有原始回执、Usage、失败和费用未知项；用户最终决定另外记录，不自动采纳结论、交易或发布。
