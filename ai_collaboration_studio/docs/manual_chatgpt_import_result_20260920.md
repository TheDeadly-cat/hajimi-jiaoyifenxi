# ChatGPT 真实返回导入记录

2026-09-20，用户提交了真实 ChatGPT 返回附件，包含两轮 Panel、8 个角色视角和最终综合结果。通过正常导入服务完成确定性校验，独立手工协作副本进入 `API_REVIEW`：表示已导入、等待独立 API 复核，不表示复核已经执行或用户已确认结论。

## 身份和材料

- 本轮导入使用源码 `75bed56055fbe3ce1dd74941900bf7f06c414483`；本结果文档为后续提交。
- 用户声明的模型为 `gpt6pro`，按原文记录；`declared_model_trusted=false`，没有通过 OpenAI API 或供应商日志核验模型身份。
- 冻结包 SHA-256 为 `5f311f0949837268a57b795fe74b8f2e9d1e3933ca157cb76dd05cca3e4d1dfd`，上下文 SHA-256 为 `4045b0ed77e7d192975c42579f9d4bada2c537cfbb5014f734d51c2ddc5c6e5c`，均与返回相符。
- 两个 Panel 分别为 synthesis、counterargument，各包含冻结角色表的 4 个视角。两个连续回答及同一回答内的角色视角都不能当作独立模型票数；所有独立性声明继续标记为不可信元数据。
- 合并范围仍是同一正文版本的 3/16 段，未读 13/16 段；本轮没有追加来源抓取或模型调用。

## 原件、格式修复与实际导入

用户消息展示了第一个 Panel，但附件已经包含两个 Panel 和最终综合结果，因此没有要求用户重新生成第二轮。

1. 原始附件按字节保留。原文中 8 个 `:chatgpt-content-reference` 展示标记带有未转义的引号，破坏 JSON 字符串；正常导入器以 `JSON_PARSE` 拒绝，拒绝事件保留。
2. 在单独副本中仅删除这 8 个精确标记。研究文字、Panel、角色视角、证据引用和建议均不改变。清理后预检唯一问题是 `declared_model` 为空。
3. 用户随后明确提供 `gpt6pro`。仅补入这一声明字段；原分析中“此前未提供模型名称”的历史表述仍保留，补充时间和来源另行记录，不重写原分析。
4. 本机导入驱动最初误传了解析后的对象，服务要求 JSON 文本，因此第二次以 `TYPE` 拒绝。该问题属于本机驱动，不是用户结果或生产校验器故障；拒绝事件和首个驱动版本保留。驱动改为传入同一对象序列化后的 JSON 文本，未触发 API 请求。
5. 正常服务完成 `RESULT_IMPORTED → VALIDATING → API_REVIEW`，确定性校验无问题。没有放松原合同、修改后端校验或直接改写会话状态来通过导入。

## 检查结论

房间、轮次、冻结包、上下文、Panel 顺序和类型、角色覆盖及合法证据引用均通过原有校验。正文保留了电话会计划与财报发布时间的区别、回放约数和对象限定、一般性变更说明与实际改期的区别；结论范围为冻结选段文字核对，外部真实性及当前有效性没有被升级为已验证。

确定性校验验证的是结构及引用绑定，不验证用户声明的真实模型、盲审独立性、现实世界真伪或所有分析语义。用户还没有作最终选择，导入结果也不等于完成后续独立复核。

## 隔离和费用

本轮禁用全部 Provider，并在进程内阻止网络连接，观测到网络尝试 0、API 调用 0。独立手工数据库仅 `manual_chatgpt_sessions` 和 `manual_chatgpt_events` 两表变化；其他表与原备份一致。复核运行、复核结果、决定、调用运行和调用尝试表均为 0。正常 checkpoint 后没有 WAL/SHM 残留。

原证据备份和此前豆包、智谱、DeepSeek 共三次调用的独立 API 数据库哈希均保持不变。没有把那三次连通性测试移作此结果的独立审查，也没有使用冻结任务预算自动授权后续付费请求。正式数据不受影响，OpenAI API 硬禁用保持不变。

## 审计哈希

| 对象 | SHA-256 |
| --- | --- |
| 用户原始附件 | `7fccad758e5a443c1738ae8ca19085f100738612172bc79f6a3a32e8fc336074` |
| 仅清理展示标记的副本 | `1b1d3a8f5ffbd07017fc2c04af88d2456374a9c241e1d8c48375fa9f619d75f9` |
| 补入用户模型声明的导入文件 | `a942c10e7183931db48cb2dceda1a09cbc63dbfc6bf64047ef9f096a40f5a077` |
| 服务规范化后的结果 | `332e48ab2772b516b01e24664cc3f2a6a932d738260fa98549977a6fa500b427` |

原附件、完整结果、修复记录和数据库仅留在本机独立目录，不随此工程摘要上传。此次无需修改生产代码或重新运行完整回归；已有 Futu 回归问题与当前导入试验分开记录。
