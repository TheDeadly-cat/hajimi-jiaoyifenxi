# 豆包首次真实 API 联调记录

2026-09-20，首次受控请求成功返回，实际模型、JSON 结构、引用和 Usage 均通过本次工程检查。回答仍为未经核实的模型输出；发现语言和读取范围表述问题，不代表完整质量验收。

## 本次身份与范围

- 实际执行源码：`9cb1f8c0d11efad1cfbd1898c88bd0029b4528a5`。本记录是在执行后补充，不能将后续文档提交冒充已实测版本。
- Provider：火山方舟豆包；端点 `https://ark.cn-beijing.volces.com/api/v3/responses`。
- 请求及实际返回模型：`doubao-seed-2-1-pro-260915`。
- 用户明确批准一份计划、最多一次请求，输出上限 8,192 Token，消费计划不超过人民币 1 元；失败不重试，不切换模型。
- 输入是独立数据库副本中的既有 Micron 冻结材料：同一公告、同一正文版本的两份选段，分别 1,033 和 1,448 字符。合并后为 `p0001`、`p0002`、`p0005`，共 16 段中的 3 段；附件与其他段落未读。
- 完整 HTTP body 9,940 字节；输入限制 16,384 字节；超时 240 秒。未抓取 SEC/Micron，未运行其他模型、正式讨论或人工结果导入。

## 返回与用量

| 项目 | 实际结果 |
| --- | --- |
| HTTP 请求 | 1 次，未追加探测或重试 |
| 完成状态 | `RESPONDED`；调用账本 `COMPLETED`，预留及实际尝试均 1 |
| 输入 Token | 4,937，缓存命中 0 |
| 输出 Token | 689，报告的思考 Token 为 0 |
| 总 Token | 5,626 |
| 耗时 | 15,875 ms |
| 估算费用 | 0.050292 元，按未抵扣免费额度、输入全部未命中缓存计算 |
| 供应商账单 | 未核实；不能声称实际扣费为零或等于估算金额 |

计价核对：[火山方舟官方价格](https://docs.volcengine.com/docs/ark/model-pricing)，Seed 2.1 Pro 常规文本输入 6 元/百万 Token，输出 30 元/百万 Token。截图显示的 50 万 Token 体验额度不替代请求发生后的账单核对。

## 内容检查

三条事实分别是财报电话会议安排、官网直播和约一年回放期限。引用均来自获准 evidence ID，且逐字存在于相应选段中。回答没有编造收入、利润、指引或板块方向；明确材料不足、附件和其他正文未读。

保留两项质量问题，不补发请求：

1. 回复为英文。本次中文提示未明确约束回答语言；后续中文产品使用应显式要求中文。
2. 回复把未读段落数表述为“14 或 15”，未按两个选段的合集计算。合并后的准确范围是已读 3/16、未读 13/16；这影响范围表述质量。引用校验通过不代表这类数量表述正确。

源回执仍保留 `claim_status=unverified_model_output`，其原始 `content_quality_review=not_performed` 不被改写。以上是请求结束后单独记录的检查，不是人工研究导入、独立 API 复核或投资结论确认。

## 数据、密钥与过程边界

原备份 SHA-256 保持不变；副本的逐表内容只有调用运行和调用尝试两表新增记录，其余表保持一致。研究任务仍为 `BUNDLE_READY`，没有改为 API_REVIEW 或生成正式轮次。结束后正常 checkpoint，WAL/SHM 不残留。

密钥由用户在本机进程内输入，不进入聊天、文件、命令参数、前端或 GitHub；测试进程退出后不保留常驻密钥配置。初次控制台输入遇到窗口不可见、Windows PowerShell 5 的 UTF-8 读取问题及隐藏输入误解，均没有模型请求记录。最终用本机圆点密码框完成输入；辅助窗口仅改变输入呈现，原 CLI 仍检查候选、计划哈希、端点、有效期和单次账本。辅助窗口为本次本机操作工具，尚未作为项目通用安装入口交付。

本次 29 项定向离线测试通过、外部连接 0；静态检查 7/7 通过。未重跑全量回归。此前两项 Futu worker stop 测试失败仍单独记录，本次不能宣称全量回归或新远端 CI 已通过。

## 审计摘要

- 授权计划 SHA-256：`01e5465e55ddc5f150f212eac0e167a87ea58460596b12bc5462ab680dc2d6d7`。
- 请求体 SHA-256：`43363eb2586185cdeaf417bd81277d3251dbc0d055a677294fcf67d86dbbbf13`。
- 原冻结包 SHA-256：`5f311f0949837268a57b795fe74b8f2e9d1e3933ca157cb76dd05cca3e4d1dfd`。
- 完整执行记录文件 SHA-256：`c8b96010fd42bd43a0e2dc5008c5840b6e28980a743180243b00d2420f4b800e`。
- 原备份 SHA-256：`7801cef85a945ce469e03ea8ebdf127b1bd9811420c77cca74d1c065545a4ada`。

完整输入、输出、运行 ID 和本机辅助记录保留在独立试验目录，不随源代码上传。千问、智谱及方舟托管的其他模型本轮未作真实调用；不因此认定它们已经接通。一次成功也不等于自动监控分析、无人值守运行或公开发布完成。
