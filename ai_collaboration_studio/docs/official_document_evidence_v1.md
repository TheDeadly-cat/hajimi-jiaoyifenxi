# 官方正文证据与可阅读事件卡 v1

本功能在 `c353d7144dda8559faf68439e5dc8a51bfcfefae` 候选上增加正文证据和确定性原文展示，不接入 AI 自动初评。原来的元数据监控、来源指纹、规则 sidecar、checkpoint、已阅、房间挂接和轮次草稿协议均不改变。

## Micron Evergreen 正文适配（2026-09-13）

真实历史公告的 HTML 已下载，但初版解析结果为空：正文使用精确的 `evergreen-news-body` class，并位于 ASP.NET 页面级 `<form id="fmForm1">` 中。初版既不识别该容器，也会忽略整个外层 form。

解析器 `official_html_blocks_v2` 增加这个精确 class，并只允许 Micron 的上述页面 form 承载文章；form 本身不授予正文范围。普通搜索/订阅 form、文章内部的 form、导航、隐藏祖先和脚本仍排除，SEC 的 form 处理保持原样。没有放宽 URL、DNS 公网地址、TLS、重定向、字节或次数限制。

解析器版本参与证据身份。旧 v1 记录和段落引用继续可读且不修改；v2 可以形成独立版本，同一 v2 内容的再次检查仍去重。没有增加表或迁移标记。

新增的页面 form / 正文容器回归在修复前确实失败。修复后 90 项隔离后端定向测试通过，覆盖旧版本不变、相同新版去重、隐藏/导航/其他 form 和 SEC 边界。对授权下载的原始 HTML（SHA-256 `9b1384bf830574a7d6b2d6fc159b664d4cd97f82dda20a0824efd3ada4ff1d1e`）离线重解析得到 16 段、1,547 字符，与禁用脚本和外部资源后的原文 DOM 在空白归一化后相同。保留链接与附件未读警告。这是离线解析修复证据；实时运行、人工资料交接和最终提交 CI 另行记录，不由本段推定通过。

实机后续：修复候选 `0ecd1dc097a18a68d8573c952aa498dbbae23769` 在同一独立库再次明确读取该 Micron HTML，产生 v2 的 16 段正文。v1 的空正文版本仍保留；v2 原始字节哈希与上段相同，正文 SHA-256 为 `dac7ec279650fe29a75348f308c7d031cb67253cea0032319f2fe580c0b00e8f`。状态维持 partial，原因是链接与附件未读，不代表文章文本缺失。已打开详情自动更新，复制内容保留版本与段落引用。

人工交接通过既有网页操作完成：明确建立独立研究房间，将前两段连同来源、两种哈希、获取时间、解析器版本及警告作为 1,168 字符的选段资料保存，再冻结人工 ChatGPT 任务包。保存文本与任务包 `evidence_index.excerpt` 逐字符相同，没有触发每份 1,600 字符上限。其余 14 段明确未进入研究包；手工笔记的结构化 `source_url` 为空，原始 URL 完整保留在选段内容及导出任务提示中。未发送到外部 ChatGPT，Provider 和正式轮次仍为零。

这轮上限为 15 分钟、三次官网请求尝试：SEC submissions 一次仍在 TLS 阶段失败，因此不再请求 SEC；Micron 一次下载揭示解析缺陷，修复和 90 项定向验证后使用剩余一次额度确认 v2。联网宿主在首次请求后约 9 分 40 秒停止，之后研究交接在离线宿主中进行。本机经授权的域名 DNS 例外处理与业务代码分开保留；未降低 DNS 公网校验、TLS 或来源边界。SEC 8-K 正文真实验收仍未通过，不能以 Micron 成功代替。完整 CI 以最终推送提交的终态为准。

## 用户操作

打开「来源收件箱」，选择一条已导入的 NVDA 8-K 或 Micron 官方事件，在详情中的「官方正文证据」区域操作。

1. 查看状态不会访问来源网站。勾选范围确认框，再点「确认读取这条正文」，才安排这一条的读取。
2. 正文在独立后台任务中读取，页面显示等待、读取中、完成、部分内容或失败；不必启动讨论。
3. 读取后的卡片显示直接相关公司、实际读取范围、获取时间、原文段落和未知项。8-K 主文件的附件没有读取，会明确显示「当前证据不完整」。
4. 「正文证据版本」可以查看旧版本。每个段落的引用 ID 绑定不可变版本；复查不会覆盖旧引用。
5. 点击「复制本版正文研究材料」可复制标题、来源、范围、版本和段落引用。在目标房间的「共享资料」中手动加入后，复用已有 ChatGPT 协作任务包。下方原有「附加到房间」仍只附加原事件，不悄悄改变旧材料。

需要处理之后的新事件时，展开收件箱上方「正文自动补全（默认关闭，需单独确认）」。检查说明，勾选后点「确认启用新事件正文补全」。开启时只记录当前事件水位，历史事件不回填。不会开启原本关闭的元数据来源监控。

自动授权每次最多 20 分钟、合计 6 个正文操作；同时还以数据库记录限制所有手动和自动操作在滚动 20 分钟内合计最多 6 次。队列预约也计入上限。到期不开始新读取，在途正文读取接受截止时间控制；重启不恢复授权。不提供常驻无限正文采集。提前结束时按既有操作说明正常停止所属宿主控制台；关闭网页不会停止后台。

### 排队与详情状态修复（相对 b5f3747）

- 排队时通过检查不代表执行时仍获准。发送前重新核对最新同源冷却、取消信号、授权截止；事件完整性读取结束后再次检查。若同源任务先收到限流，后续尚未发出的任务明确取消，保留原因并要求用户手动确认。取消不退还已经预约的额度；失败任务不自动重试，队首取消后另一合法发布者仍能继续。
- 自动扫描遇到冷却或预算不足时，保存该事件的「未预约，需手动确认」记录后才推进正文扫描游标。即使六次额度已用完，在当前授权窗口内仍会为随后扫描到的事件记录明确处置，每次最多扫描 20 条；不会发生额外来源请求。该游标不是元数据 checkpoint，原事件不变。
- `document_job_` 记录仍代表真实预约，不因取消、失败、重启或重新授权而退费。`document_disposition_` 记录仅代表从未预约、从未发送的终态处置；不消耗额度，也不允许执行。相同事件已有记录时复用，不重复插入。冷却或额度恢复后仍需用户手动确认，不能将这些记录变为无限自动重试。
- Retry-After 接受秒数与 HTTP 日期；缺失或非法输入维持至少五分钟的保守等待。超出可表示时间的有效延迟使用 SQLite 最大整数作为「人工处理暂停」标记，永不按这个数字安排重试或显示无效日期，不将超大延迟缩短为五分钟。遵循 [RFC 9110 的 Retry-After 语义](https://httpwg.org/specs/rfc9110.html#field.retry-after)。
- 已打开详情通过已有刷新信号和授权期间的有限本地 GET 更新，即使首次为「尚未读取」也能随后显示后台结果。通常每五秒读取一次；已有等待/获取中任务沿用 1.5 秒读取并受授权/任务截止及短暂收尾窗口限制。切换事件、卸载都会取消旧读取；到期更新自动开关状态。也可随时点「刷新本地正文状态」。这些都是本机状态读取，不会 POST、抓取来源或调用 Provider。

所有访问限于事件已经绑定的 NVDA 8-K 主 HTML、Micron `/news/press-release/<year>/<slug>/default.aspx`。不接受任意 URL，不跟随重定向，不遍历附件、历史目录、PDF、PPT 或其他公司。

## 实现与证据语义

`backend/document_evidence.py` 提供受限提取、版本保存、持久任务与一个宿主拥有的后台工作线程。`http_server.py` 只接入本地状态/明确确认接口及启动收尾；元数据 Runtime 不等待正文网络工作。默认关闭时工作线程不轮询数据库或来源。

- 复用 `material_ingest` 的公开地址解析及 DNS 固定连接、编码处理、FetchedResource；复用 `official_http` 的有界响应读取、取消和截止控制。不调用会向房间写材料的 `MaterialIngestService.fetch_url()`。
- 本版正文连接采用已有材料读取器的 DNS 固定直连传输，不继承浏览器登录或代理会话，不修改系统或进程代理设置。SEC 使用本机既有 SEC_USER_AGENT；没有配置时明确失败。正文传输与元数据适配器的代理链路分别验收：Micron 单条历史 HTML 已有上述实机证据，SEC 正文仍未通过；不能推广为所有来源或页面都可达。
- 单操作最多一个 HTTP 请求，无自动重试或重定向请求；1.5 MB 响应上限，50,000 正文字符上限。字数截断会明确标为部分内容。正文没有可识别的文章容器、只有 NewsArticle 页头或找不到足够文本时，不生成伪全文。
- 正文网络/解析不持有 SQLite 写事务；版本插入和任务完成在同一事务中提交。正文失败不能回滚已经提交的来源事件。
- 重复确认返回已有任务，不再次 HTTP 读取；显式复查最少间隔 5 分钟。同一事件、URL、原始字节、正文及解析器版本形成幂等身份。相同内容复查保存最新任务结果而不重复增加版本；旧版本的首次获取时间保持原样。
- 429/503 的 Retry-After 保存并作用于同一发布者的其他事件，不能换事件绕过等待。失败或中断只在用户再次明确确认后重试；不对可能已发送的未知请求声称“恰好一次”。
- 单次网络预算为 12 秒或授权剩余时间的较小者。响应读取可以取消；系统 DNS 阻塞等未返回的底层调用仍可能超出收尾等待。超过 15 秒未停止会保留数据库 owner 并失败关闭，不宣称严格时限下全部进程退出已验收。
- HTML script/style/隐藏内容不执行；可见的恶意指令仍是无权限的外部文本，React 按文本展示。摘录不是 AI 总结，不给涨跌判断、交易授权或收益概率。

每个 `official_document_evidence_v1` 记录包含原事件 ID/指纹、请求及最终 URL、发布/修订时间（未知为 null）、本地获取时间、原始字节 SHA-256、规范正文 SHA-256、实际解析器版本（历史记录为 `official_html_blocks_v1`，本次适配后为 `official_html_blocks_v2`）、读取范围/警告、未访问的正文链接清单、原文和稳定段落 ID。正文两种哈希不复用既有元数据哈希。段落按块顺序保存，表格行保留单元格分隔；没有进行财务表格语义识别。

本次修复没有新表或新字段，也没有更改正文证据格式、解析器版本或旧引用。复用任务表的 cancelled 状态，以独立 ID 前缀和明确错误码区分「未预约处置」与「已预约后取消」。旧 b5f3747 可以读取同一记录结构，但会将未预约处置也计入额度而保守拒绝；它不包含本次执行时冷却修复，因此不是安全恢复正文自动执行的目标。返回 b5f3747 时应保持正文自动功能关闭。正文修复的最低运行版本为本节对应修复提交；没有改写迁移标记来伪装新增 schema。

## 数据与兼容

新增 `source_document_jobs` 和 `source_document_versions`，以及迁移标记 `official_document_evidence_v1`；版本记录有禁止 UPDATE/DELETE 的触发器。任务状态可以变化，正文版本不可变。没有更改旧事件、旧 sidecar、旧 checkpoint 或房间材料版本。

正文 reader 从本功能提交起支持 v1 证据记录；本次 v2 解析器仍使用同一记录结构，并保留旧记录。早于正文功能的候选只能处理原有元数据；不把“旧元数据仍存在”当作新功能可被旧版读取的证明。新增结构通过原有数据库迁移 manifest/shadow/授权流程识别，启动不自动升级已有数据库。初版开发只初始化隔离临时库；后续试用另行准备了独立持久目录，均未迁移正式数据库、RC1 或 OnlineCollection 数据。

已有桌面离线/联网入口仍属于 c353d71 元数据候选。功能分支不自动改绑这些版本配置。实际试用正文版需另行准备匹配的新入口和明确的数据升级范围；不能复制旧配置中的候选 SHA 或构建哈希假装兼容。回退目标是升级前独立备份和匹配的旧代码/配置；不得删除 WAL 或将新库强行降级。

## 本地验证（Windows，隔离 fixture）

执行命令：

最终定向结果：后端 73 项通过，失败/错误/跳过均为 0；前端 3 个文件共 46 项通过，失败/跳过/取消均为 0；生产构建通过。后端网络审计 57 次允许的测试回环连接、0 次外网或子进程拦截尝试。重复开发验证与最终验证不相加为独立覆盖。

上段为 b5f3747 初版记录。本次修复先在项目自身回归中得到 4 个后端失败（D1、D2 冷却、D2 额度、超大 Retry-After）和 1 个既有日志测试错误，以及 1 个 D3 前端失败。旧 SHA 的两条完整 CI 也确认为同一日志测试错误：简化 Store 缺少新增正文服务；现已补齐该测试的服务替身，并保留原日志边界断言。

修复后定向结果：后端 87 项通过、前端 3 个文件共 50 项通过，均无失败/错误/跳过，生产构建通过。后端命令在上次模块列表上增加 `tests.test_structured_logging`；网络审计为 58 次允许的测试回环连接，外网及子进程拦截尝试均为 0。新增冷却/预算、另一发布者继续执行、取消不退费、重新授权/重启、处置写入失败、HTTP 日期、前端时间推进和旧响应隔离回归。一次前端整合测试因未展开的控制面板提前读取而出现夹具告警，断言输出触发内存保护；改为按需读取后通过，没有提高内存上限。

浏览器复验复用原隔离宿主：详情先显示 not_fetched，再由 fixture 后台自动排队并完成；页面无需重新打开或发送 POST 即显示 partial 原文。1440×1000 和 390×844 下页面无脚本异常或横向溢出；账本、正式轮次、材料、挂接与草稿均为零。GitHub 完整 CI 以本次最终 SHA 的终态为准，不能用以上定向结果或旧 SHA 代替。

```powershell
.\runtime\bootstrap\python\Scripts\python.exe -X utf8 -B scripts/run_backend_tests_isolated.py tests.test_document_evidence tests.test_store_startup_hardening tests.test_source_monitoring_runtime_host tests.test_material_ingest tests.test_monitoring_official_delivery --verbosity 1
npm.cmd --prefix frontend test -- tests/documentEvidence.dom.test.js tests/sourceInbox.dom.test.js tests/sourceInbox.test.js
npm.cmd --prefix frontend run build
```

覆盖：SEC/Micron 正文与元数据哈希分离、缺正文/页头、附件未读、重复请求、同内容复查、修订旧引用、SQLite 原子回滚、重启取消、来源范围、429/超时/取消/过大响应/禁止重定向与私网地址、后续事件继续入库、默认关闭/新事件水位/预算截止、宿主线程收尾、旧数据只读迁移预检。HTTP 测试检查会话令牌、明确确认以及零调用账本/正式轮次/房间材料；原有官方来源组合回归保留 Provider spies。

页面用现有 Playwright + Edge headless，在随机回环端口、临时 SQLite 中验证 1440×1000 和 390×844。完整交互只有一条确认后的正文 POST，原事件保持未阅；调用账本、正式轮次、房间材料/挂接/草稿均为零。浏览器宿主启用既有离线网络 guard，所有正文响应来自明确标识的合成 fixture。临时浏览器脚本、截图和原始日志不提交仓库。

开发中首次迁移时间标记错误、旧宿主测试假对象缺少新增服务边界，以及临时浏览器记录脚本调用了错误的 audit 方法，均已纠正后复验；不把首轮失败删除后声称一次通过。

上述 fixture 本身不构成真实公告发现或金融事实验证；后续单条 Micron 历史 HTML 的实机结果见本文件 Evergreen 适配节。仍未验证：SEC 正文、自然新增公告全链、其他网站 HTML 变体、系统强杀/断电恢复、全天候无人值守、付费 API、自动语义初评、正式数据升级。GitHub CI 必须按最终提交的实际运行终态另行判断。
