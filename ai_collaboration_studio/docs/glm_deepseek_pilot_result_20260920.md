# 方舟智谱与 DeepSeek 官网单次联调记录

2026-09-20，两次单独批准的真实请求均完成。智谱通过火山方舟返回 GLM-5.2，DeepSeek 通过官网返回 V4 Pro；模型身份、完成状态、JSON 合同和逐字引用通过本次检查。结果仍为未经核实的模型输出，未导入为人工研究或正式决策。

## 源码、授权与材料

- 两次实际执行源码均为干净候选 `bcf98a457851647f7dbceae5122a182a46face03`。本记录在执行后提交，不能将文档提交替代实际测试源码身份。
- 用户先误选“只批准智谱”，随即纠正并明确批准智谱、DeepSeek 各一次；真实请求在纠正后的授权到达后才开始。
- 智谱固定 `glm-5-2-260617`，使用 `https://ark.cn-beijing.volces.com/api/v3/responses`；DeepSeek 固定 `deepseek-v4-pro`，使用 `https://api.deepseek.com/chat/completions`。
- 各最多一次请求、输出上限 8,192 Token；智谱消费计划上限 1 元人民币，DeepSeek 上限 0.10 美元；有效窗口截至北京时间 07:17:01。各自失败也停止，不重试、不切换模型。
- 两次均只发送已冻结的同一份 Micron 公告的两份选段，共 2,481 字符，合并为 16 段中的 3 段，未读 13 段。附件未读，本轮没有抓取 SEC/Micron，也没有调用其他模型补充研究。
- 原冻结包 SHA-256：`5f311f0949837268a57b795fe74b8f2e9d1e3933ca157cb76dd05cca3e4d1dfd`。本次增加中文输出要求和按正文版本去重的阅读范围提示，没有改写原材料或此前豆包回执。

## 实际返回

| 项目 | 智谱 / 火山方舟 | DeepSeek 官网 |
| --- | --- | --- |
| 请求及返回模型 | `glm-5-2-260617` | `deepseek-v4-pro` |
| HTTP 尝试 | 1 | 1 |
| 完成状态 | Responses `completed` | Chat `finish_reason=stop` |
| 本机验收状态 | `RESPONDED` | `RESPONDED` |
| 输入 Token | 4,194，缓存 0 | 4,042，缓存 0 |
| 输出 Token | 1,310，其中报告思考 818 | 2,924，其中报告思考 2,095 |
| 总 Token | 5,504 | 6,966 |
| 耗时 | 25,146 ms | 18,861 ms |
| 估算费用 | 0.070232 元人民币 | 0.01691448 美元 |
| 逐字事实引文 | 2/2 匹配所选证据 | 3/3 匹配所选证据 |

智谱估算沿用用户提供的方舟控制台 GLM-5.2 价格截图：输入 8 元、输出 28 元/百万 Token；[官方价格入口](https://docs.volcengine.com/docs/ark/model-pricing)。DeepSeek 按[官网价格](https://api-docs.deepseek.com/quick_start/pricing/)的峰时、未命中缓存单价保守计算：输入 1.32 美元、输出 3.96 美元/百万 Token。未读取供应商账单，未确认免费额度、优惠时段或余额的实际抵扣，以上均不是实际扣费凭证。

## 内容检查及边界

两份回答都使用中文，并正确说明还有 13 个段落及附件未读；没有将日程公告扩写为财务数据或板块方向证据。逐字引文来自允许的 evidence ID，与对应选段一致。

智谱将“当天披露财报结果”放在条件推断中；原文直接支持的是电话会议日程，不能将这条推断改写为已经核实的披露承诺。DeepSeek 的第三条事实是版权声明，对研究结论的帮助有限；其回放推断也与已引用事实有重复。工程合同通过不代表完整内容质量验收。

原响应保留 `claim_status=unverified_model_output` 和 `content_quality_review=not_performed`。本节是请求完成后的独立检查，不回写原始模型响应，不触发独立 API 审查、正式讨论或用户决策确认。

## 接入和密钥输入改动

智谱仍使用原 `glm` 身份，通过明确的 `glm_platform=volcengine_ark` 选择方舟通道；未指定时保留智谱官网行为。该通道按 Responses 协议检查完成状态，不能误用官网 Chat 的 `finish_reason`。它以提示约束 JSON，不声称支持原生 JSON Object，也不发送额外能力探测请求。模型默认思考模式保持不变。

操作脚本增加本机圆点密码框，支持 Ctrl+V；用户只在独立窗口输入，各测试进程结束后清除进程内密钥变量。密钥不写入聊天、文件、命令参数、GitHub 或常驻配置。本次两次窗口均有已打开、已提交和正常退出的无密钥记录。

## 验证与数据隔离

- 首轮定向检查为 47 项，其中 1 失败、1 错误：暴露了方舟智谱完成状态使用错误协议，以及 HTTP 测试替身的签名不匹配。两项已修复，原失败日志保留。
- 修复后 47 项定向检查通过；另增加并运行 1 项“Responses 不能被 Chat 完成标志替代”的独立回归，通过。共 48 项不同检查，不将重复执行叠加计数。
- 静态检查 7/7 通过；上述离线测试外部连接为 0。随后密码框尺寸调整不改变 HTTP 请求体；两次真实调用均在最终候选 `bcf98a4` 完成。
- 原备份 SHA-256 仍为 `7801cef85a945ce469e03ea8ebdf127b1bd9811420c77cca74d1c065545a4ada`。独立 API 数据库逐表比对仅调用运行、调用尝试两表改变，其余表与原备份一致，研究任务仍为 `BUNDLE_READY`。
- 该副本累计记录豆包一次、智谱一次、DeepSeek 一次，已达到三次总上限；没有追加探测或重试。数据库完整性检查通过，正常 checkpoint 后无 WAL/SHM 残留。
- 本轮没有重跑完整回归；此前两项 Futu worker-stop 回归失败仍单独保留，不能由这些定向检查或 API 成功覆盖。远端 CI 状态应按本次提交单独核对。

## 另外两条路径

千问按用户明确选择保留为个人编程套餐，不在项目后端消费 Token Plan；通用百炼适配器的存在不代表此次已测试千问 API。

GPT 沿用用户确认的手工 ChatGPT 协作：独立副本中已准备两轮任务的导出提示，第一轮综合分析、第二轮反方检查并返回最终 JSON。未收到用户真实返回，当前没有记录已发送、没有导入结果，也没有启动冻结包中描述的后续 API 审查。OpenAI API 硬禁用保持不变。这条路径仍待用户在 ChatGPT 完成两轮后返回 JSON，再做正常导入校验。

## 审计摘要

| 记录 | 智谱 / 火山方舟 | DeepSeek 官网 |
| --- | --- | --- |
| 计划 SHA-256 | `d767f538de3b459142e0fe9b961612c4675e21b9278ecad859c582d1a9da344a` | `32d91b7f410f51d27283a9d196bec9c6c08b5c9512d4256f95ae020d46d3fdbf` |
| 完整请求体 SHA-256 | `597ee2bd5ac0b66ba21eaff2dc26522e4bf28f5e8dd17136242c883575931b9d` | `0f1b3d2fc158b408d481ae4ac8c1ebeb86a0ac127dcd1506f960c2cb76f1be75` |
| 完整执行文件 SHA-256 | `c764e892df7a3483962c0846d0e4cad2dbe519ddf0b558393fdbb21883e93244` | `bde5cc499fbd0f1412d2a65d7c6328b3ea6df2591b7a79c299566d4db75c82ef` |

完整输入、模型返回、数据库和本机操作记录仅保留在独立试验目录。此文档公开工程结果和哈希，不上传密钥或运行数据；两次单次成功不等于无人值守运行、完整回归或公开发布验收。
